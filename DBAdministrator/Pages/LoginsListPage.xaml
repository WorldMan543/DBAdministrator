<Page x:Class="DBAdministrator.Pages.LoginsListPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
	  DataContext="{Binding RelativeSource={RelativeSource Self}}"
	Title="LoginsListPage">
    <Grid>
		<ListView ItemsSource="{Binding Models}" Margin="15">
			<ListView.View>
				<GridView>
					<GridViewColumn Header="Name" Width="120" DisplayMemberBinding="{Binding Name}" />
					<GridViewColumn Header="Default Database" Width="120" DisplayMemberBinding="{Binding DefaultDatabase}" />
					<GridViewColumn Header="Language" Width="120" DisplayMemberBinding="{Binding Language}" />
					<GridViewColumn Header="Login Type" Width="120" DisplayMemberBinding="{Binding LoginType}" />
					<GridViewColumn Header="Server Access" Width="120" DisplayMemberBinding="{Binding ServerAccess}" />
				</GridView>
			</ListView.View>
		</ListView>
	</Grid>
</Page>
